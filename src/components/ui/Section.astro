---
/**
 * Section component for Lime Brothers Co website.
 * A semantic page section with consistent vertical spacing.
 */

interface Props {
  as?: 'section' | 'div';
  id?: string;
  class?: string;
  'aria-labelledby'?: string;
}

const {
  as: Tag = 'section',
  id,
  class: className = '',
  'aria-labelledby': ariaLabelledBy,
} = Astro.props;
---

<Tag id={id} class={`section ${className}`.trim()} aria-labelledby={ariaLabelledBy}>
  <slot />
</Tag>

<style>
  .section {
    padding-block: var(--section-spacing);
  }
</style>
